def print_histogram(data)
  max_value = data.max
  data.each do |num|
    puts "#{num}: #{'*' * (num * 20 / max_value)}"
  end
end

# Example usage
numbers = [5, 12, 8, 3, 15, 10]
puts "Histogram:"
print_histogram(numbers)
